---
trigger: always_on
---

Use existing architecture and design patterns, keep files under 600 lines, reuse components and logic instead of duplicating code, and avoid unnecessary dependencies. Always verify the actual codebase. Follow .agents/design.md, maintain UI consistency across devices, preserve accessibility, and avoid unnecessary complexity. For Supabase, inspect schemas first, reuse existing structures, never disable RLS, and follow least-privilege principles. Treat all client input as untrusted, validate server-side, enforce authorization and ownership checks, and never expose secrets. Optimize for performance by avoiding unnecessary queries and requests, using pagination and efficient data access patterns. Most importantly, only change what the user requested and do not break or modify unrelated functionality. Sanitize every inputfields, limits in input fields.
You have direct access to the backend via mcp server to supabase fetch info directly from the backend and apply migrations/sql directly to the backend.
If there is any feature that has not been mentioned clearly then act as how the big tech would have perform and made that feature or the thing that i have not specified.