<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <!-- Content Viewport (where we swap ImageView, PlayerView, WebView) -->
    <FrameLayout
        android:id="@+id/content_viewport"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Control Overlay (Fullscreen and Menu buttons in top right) -->
    <LinearLayout
        android:id="@+id/control_overlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_margin="24dp"
        android:orientation="horizontal"
        android:gravity="center">

        <!-- Fullscreen button -->
        <ImageButton
            android:id="@+id/btn_fullscreen"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/control_button_bg"
            android:src="@drawable/ic_fullscreen"
            android:scaleType="fitCenter"
            android:padding="10dp"
            android:tint="@color/white"
            android:contentDescription="Toggle Fullscreen" />

        <!-- Hamburger button -->
        <ImageButton
            android:id="@+id/btn_menu"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="16dp"
            android:background="@drawable/control_button_bg"
            android:src="@drawable/ic_menu_hamburger"
            android:scaleType="fitCenter"
            android:padding="10dp"
            android:tint="@color/white"
            android:contentDescription="Open Sidebar" />
    </LinearLayout>

</FrameLayout>
