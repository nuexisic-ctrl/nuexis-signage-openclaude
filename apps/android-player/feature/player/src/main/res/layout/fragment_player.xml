<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Background Player (For preloading) -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/playerViewBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="invisible" />

    <!-- Active Player -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/playerViewActive"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Image View for Image Assets -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:visibility="gone" />

    <!-- WebView for Website Widgets -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- Loading / No Content / Error State -->
    <LinearLayout
        android:id="@+id/statusContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <ImageView
            android:id="@+id/logoView"
            android:layout_width="180dp"
            android:layout_height="60dp"
            android:src="@drawable/nuexis_logo"
            android:contentDescription="NuExis Logo"
            android:alpha="0.5" />

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginTop="24dp"
            android:indeterminateTint="#094CB2" />

        <!-- Error icon (hidden by default, shown on error) -->
        <ImageView
            android:id="@+id/errorIcon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginTop="24dp"
            android:src="@android:drawable/ic_dialog_alert"
            android:contentDescription="Error"
            android:visibility="gone" />

        <TextView
            android:id="@+id/statusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Syncing content..."
            android:textColor="#66FFFFFF"
            android:textSize="14sp"
            android:gravity="center"
            android:maxWidth="400dp" />

        <!-- Error details (hidden by default) -->
        <TextView
            android:id="@+id/errorDetailText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="#66FF6B6B"
            android:textSize="12sp"
            android:gravity="center"
            android:maxWidth="400dp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- ComposeView for Overlay UI (Sidebar, Controls) -->
    <androidx.compose.ui.platform.ComposeView
        android:id="@+id/composeOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</FrameLayout>
